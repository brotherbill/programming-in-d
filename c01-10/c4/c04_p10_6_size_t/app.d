import std.stdio;

void main()
{
	 writeln("size_t.stringof: ", size_t.stringof);
	 writeln("size_t.sizeof  : ", size_t.sizeof);
	 writeln("size_t.min     : ", size_t.min);
	 writeln("size_t.max     : ", size_t.max);
	 writeln("size_t.init    : ", size_t.init);
}
